#include <iostream>
using namespace std;

int main(){
    // cuando algo inicia y finaliza se llama bucle o ciclo
    // cuando algo no tiene un final se llama Loop
    // i = iteración, j = i, c = contador
    // (inicio; hasta; pasos)
    // i++ = i=+1; i=+2

    int contador;

    for(int i = 1; i <= 10; i++){
        contador = i;
        cout << contador << endl;
    }
}